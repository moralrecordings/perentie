[constants]
arch = 'i586-pc-msdosdjgpp'

[binaries]
c = arch + '-gcc'
ar = arch + '-ar'
strip = arch + '-strip'
ranlib = arch + '-ranlib'

[host_machine]
system = 'msdos'
cpu_family = 'x86'
cpu = 'i586'
endian = 'little'
